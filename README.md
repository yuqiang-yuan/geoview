# geoview

A parser and renderer for `.ggb` files, rendered on Canvas 2D.

Renders geometric constructions from `.ggb` files (GeoGebra's file format — a zip archive containing `geogebra.xml`) without requiring the GeoGebra application.

## Status

Early development — Phase 1 (independent object parsing).

## License

MIT
